rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Completely permissive rules for testing - allow all operations
    match /{document=**} {
      allow read, write, create, update, delete: if true;
    }
  }
} 